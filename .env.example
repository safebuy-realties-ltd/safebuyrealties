# Copy to .env.local for local frontend dev.
# Requires local backend on :3001 — see docs/LOCAL_DEVELOPMENT.md
# Vite proxies /api/v1 → http://localhost:3001 (vite.config.ts); keep same-origin for cookies:
VITE_API_URL=/api/v1
