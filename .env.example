# Copy to .env.local for local frontend dev.
# Vite proxies /api/v1 for same-origin cookies (vite.config.ts).
VITE_API_URL=http://localhost:3000
# Default proxy target is local NestJS (:3001). To use the Render API instead:
# SBR_API_PROXY_TARGET=https://safebuyrealties-yc0i.onrender.com
