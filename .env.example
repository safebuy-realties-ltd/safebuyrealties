# Copy to .env.local for local development.
# Use the Vite dev proxy (vite.config.ts) so session cookies stay same-origin.
VITE_API_URL=/api/v1
# Optional: call the API directly without the proxy
# VITE_API_URL=http://localhost:3001/api/v1
